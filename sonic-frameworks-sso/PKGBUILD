# Maintainer: callmetango
# Contributor: Antonio Rojas <arojas@archlinux.org>
# Contributor: Andrea Scarpino <andrea@archlinux.org>
# Contributor: Balló György <ballogyor+arch at gmail dot com>

pkgname=sonic-frameworks-sso
pkgver=8.61
pkgrel=1
pkgdesc='A D-Bus service which performs user authentication on behalf of its clients'
arch=('x86_64')
url='https://github.com/Sonic-DE/sonic-frameworks-sso'
license=('LGPL')
depends=(
	'glibc'
	'libgcc'
	'qt6-base'
)
makedepends=(
	'cmake'
	'doxygen'
	'graphviz'
	'ttf-font'
)
provides=('signond')
conflicts=('signond')
groups=('sonicde')
source=("$pkgname-$pkgver.tar.gz::${url}/archive/refs/tags/${pkgver}.tar.gz")
sha256sums=('897ed793e210227afea7a4996a15e6b58ce4912001396d9f70ec91f58d28a2cf')

build() {
	cmake -B build -S "$pkgbase-$pkgver" \
		-DCMAKE_INSTALL_PREFIX=/usr
	cmake --build build
}

package() {
	DESTDIR="$pkgdir" cmake --install build

	# Do not ship docs
	rm -rf "$pkgdir"/usr/share/doc
}
